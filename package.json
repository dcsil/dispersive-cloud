{
    "name": "aws-app",
    "description": "aws-app",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.437.0",
        "aws-xray-sdk-core": "^3.2.0",
        "istanbul": "^0.4.5"
    },
    "devDependencies": {
        "jest": "^24.7.1",
        "nyc": "^15.1.0"
    },
    "scripts": {
        "test": "jest",
        "coverage": "nyc --reporter lcov npm run test"
    }
}
